// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  db: 'https://footballdb.gabercujes.com/api',
  googleLoginProvider: '501636115719-6agkr3dcrhs7jpd0pl5dfuei7pqjbkg1.apps.googleusercontent.com',
  facebookLoginProvider: '294668175345203',
  amazonLoginProvider: 'amzn1.application-oa2-client.c556fb7f5e63425580ff821df5733a88'
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
